DATABASE_URL=postgres://signal:signal_local_only@localhost:5432/signal
POSTGRES_PASSWORD=signal_local_only
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
APP_URL=http://localhost:3000
OWNER_EMAIL=owner@example.com
OWNER_PASSWORD=
TEAM_NAME=Studio Collective
# Explicitly approved domains only, comma-separated, no wildcards.
CONNECTOR_ALLOWED_HOSTS=

# Vercel/Next proxy target. Example: https://signal-api.onrender.com
BACKEND_URL=
